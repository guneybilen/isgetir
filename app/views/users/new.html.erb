

<h2><%= t('general.new_user') %></h2>
<div class="menu">
  <% if !logged_in? %>
    <%= link_to t('general.login'), login_path %>
  <% end  %>
</div>
<%= render 'form' %>
<br />
<%= link_to t('general.link_to.back'), root_path %>

