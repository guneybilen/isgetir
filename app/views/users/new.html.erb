

<h1>New user</h1>
<div id="log_in">
  <% if !logged_in? %>
    <%= link_to "Log in", login_path, {:class => "for_black_font_link"} %>
  <% end  %>
</div>
<%= render 'form' %>
<%= link_to 'Back', root_path, {:class => "for_black_font_link"} %>