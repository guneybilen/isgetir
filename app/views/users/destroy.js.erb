$(".users").html("<%= escape_javascript(render(:file => "users/admin_delete.html.erb")) %>");

var batin = $("input:submit");
batin.button();

batin.on ('click',  function() {
    batin.attr("disabled", true);
});