$(".users").html("<%= escape_javascript(render(:file => "users/admin_manage", :formats => [:html])) %>");

var batin = $("input:submit");
batin.button();

batin.on ('click',  function() {
    batin.attr("disabled", true);
});